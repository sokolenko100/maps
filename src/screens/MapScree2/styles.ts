import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  map: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  label: {
    width: '100%',
    height: '5%',
    fontWeight: '600',
  },
  filterContainer: {
    width: '100%',
    height: '20%',
  },
});

export {styles};
